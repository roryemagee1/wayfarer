class Travelers {
  constructor(data) {
    // if (data.travelers[0].id && data.travelers[0].name && data.travelers[0].travelerType) {
    this.data = data;
    // } else {
    // this.data = "Incorrect data type(s) in Travelers class";
    // }
  }
}

export default Travelers;
