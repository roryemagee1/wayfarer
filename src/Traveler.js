class Traveler {
  constructor({id, name, travelerType}) {
    this.id = id;
    this.name = name;
    this.travelerType = travelerType;
  }

  retrieveAllTrips() {
    
  }

  totalSpentOnTrips() {

  }

}

export default Traveler;
